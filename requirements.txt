numpy>=1.24.0
cryptography>=41.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
# zkSNARK tooling required for authentication proof generation (install separately):
# circom (https://docs.circom.io/), snarkjs (npm install -g snarkjs)

